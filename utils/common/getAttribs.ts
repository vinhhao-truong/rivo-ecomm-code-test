export const getClassNames = (className = "") => {
  return className;
};

export const getStyles = (style: React.CSSProperties = {}) => {
  return style;
};

export const getId = (id = "") => {
  return id;
};
